<div class="form-group">
    <label>{{ $label }}</label>
    <input type="{{ $type }}" class="form-control {{ $class }}" name="{{ $name }}" id="{{ $id }}" placeholder="{{ $placeholder }}" value="{{ $value }}" minlength="{{ $minlength }}" maxlength="{{ $maxlength }}" {{ $required }}>
</div>